if host_machine.system() == 'windows'
  subdir('windows')
else if host_machine.system() == 'darwin'
  subdir('cocoa')
else if host_machine.system() == 'haiku'
  subdir('haiku')
else
  subdir('unix')
endif
