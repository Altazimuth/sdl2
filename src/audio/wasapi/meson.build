all_sources += files(
  'SDL_wasapi.c',
  'SDL_wasapi_win32.c',
  # I can't find how the below file is included. The CMakeLists globs
  # ${SDL2_SOURCE_DIR}/src/audio/wasapi/*.c and makes no mention of any cpp files
  #'SDL_wasapi_winrt.cpp',
)
